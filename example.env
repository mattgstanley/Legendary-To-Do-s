# Google Sheets Configuration
GOOGLE_SHEET_ID=your-sheet-id-here

# For LOCAL development: Use file path (either format works)
# GOOGLE_SERVICE_ACCOUNT_KEY=./legendary-home-481509-7918c12b8c1c.json
# GOOGLE_SERVICE_ACCOUNT_KEY=legendary-home-481509-7918c12b8c1c.json

# For VERCEL deployment: Paste the entire JSON content as a string (no file path!)
# GOOGLE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"...","private_key":"...",...}

# Email Configuration (for weekly summaries)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
FROM_EMAIL=  # Should be your actual email (usually same as SMTP_USER). Do NOT use service account emails!
FROM_NAME=Legendary Homes Task Management

# Contractor Email Mapping (OPTIONAL - emails are automatically fetched from Google Sheets Contractors tab)
# Only set this if you want to override the Google Sheets data
# Example: {"Masonry": "mason@example.com", "Plumbing": "plumber@example.com"}
CONTRACTOR_EMAILS=